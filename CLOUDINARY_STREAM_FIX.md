# Исправление битых изображений при загрузке

Что было исправлено:
- backend теперь после проверки изображения всегда возвращает поток файла в начало через `seek(0)`
- для книг добавлено отдельное поле `cover_file`, чтобы запись и чтение URL не конфликтовали
- frontend теперь отправляет файл в `cover_file`, а публичный URL берёт из `cover`

После обновления проекта:
1. залей изменения в GitHub
2. дождись redeploy на Render и Vercel
3. загрузи обложку заново
4. проверь, что URL картинки начинается с `https://res.cloudinary.com/`
